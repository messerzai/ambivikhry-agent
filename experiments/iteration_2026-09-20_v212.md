# Ambivikhry v2.12 — provenance-gated vortex lineage

Date: 2026-09-20

## Review result

The current main contains the merged personal vortex-development engine (PR #12), while the strongest provenance layer from experimental v2.11 remained isolated on its own branch. v2.12 brings that evidence layer onto current main without merging or altering PolicyGate behavior.

## Implemented

- immutable TrajectoryStep records;
- canonical serialization and SHA-256 step digests;
- order-sensitive trajectory lineage digest;
- fail-closed provenance validation;
- explicit policy states: allow, deny, approval_required;
- package discovery configuration;
- project version 2.12.0;
- regression tests for determinism, order sensitivity, missing provenance, invalid policy state, and explicit approval state.

## Tests

Local isolated execution of the new trajectory suite: 4 passed.

Local isolated execution combining the trajectory tests with the current vortex-development regression tests: 8 passed.

The repository's latest available GitHub Actions run for the v2.11 experimental head was also successful across Python 3.10, 3.11, 3.12, and 3.13, but that result is not claimed as a v2.12 result.

No hidden holdout or independent external evaluator was available, so no such result is claimed.

## Safety boundary

- main unchanged.
- No privilege expansion.
- No PolicyGate modification.
- No autonomous deployment.
- No tool hot-loading.
- No hidden-holdout disclosure.

## Status

Experimental PR #13; candidate for the next controlled evaluation gate.